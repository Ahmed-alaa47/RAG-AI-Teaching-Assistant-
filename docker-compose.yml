version: '3.8'

services:
  rag-app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./AI-Assistant:/app/AI-Assistant
    environment:
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
    command: python -m uvicorn AI-Assistant.api:app --host 0.0.0.0 --port 8000 --reload
